/**
 * 🧑‍💻 Simple test function to verify the facade can be imported and called.
 * @returns A test string.
 */
export function initializeOdinDropin(): string {
    console.log('initializeOdinDropin called from @exerp/odin-dropin facade!');
    // 📝 In the future, this will contain the real initialization logic
    // for setting up the Stencil component, OdinPay.js, etc.
    return 'Odin Drop-in Initialized (Test)';
  }
  
  // 📝 We might export a class later, like:
  // export class OdinDropin { ... }